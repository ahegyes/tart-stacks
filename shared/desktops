# shared/desktops — desktop environments the optional GUI layer can bake
# (`make build … GUI=1 DE=<de>`). One token per line; '#' comments and blank
# lines ignored. A supported DE needs a branch per package family in
# shared/scripts/gui-lib.sh. Not every distro × DE cell is buildable — the
# support matrix lives in shared/gui/README.md; an unbuildable cell fails
# loud at build time.
# Consumed by: Makefile (DE validation), bin/lib/common.sh (base-image guard).
kde
gnome
xfce
