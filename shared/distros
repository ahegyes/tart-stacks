# shared/distros — blessed distros tart-stacks builds + tests. One token per line;
# '#' comments and blank lines ignored. A blessed distro needs a published
# ghcr.io/cirruslabs/<distro> Tart image; once the provider layer lands
# (shared/scripts/distro-lib.sh) it also needs a matching family branch there.
# Consumed by: Makefile (DISTRO validation), bin/tart-new, bin/tart-up, CI matrix.
fedora
ubuntu
debian
