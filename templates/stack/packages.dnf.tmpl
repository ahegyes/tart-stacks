# __STACK__ native build dependencies (Fedora/dnf names), one or more per line;
# '#' comments and blank lines ignored. Equivalent capabilities to
# packages.apt/packages.brew (names differ); keep all three aligned. Empty if
# all runtimes are mise binaries.
