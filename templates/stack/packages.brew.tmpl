# __STACK__ native build dependencies (Homebrew formula names), one or more
# per line; '#' comments and blank lines ignored. Equivalent capabilities to
# packages.dnf/packages.apt (names differ); keep all three aligned. Empty if
# all runtimes are mise binaries.
