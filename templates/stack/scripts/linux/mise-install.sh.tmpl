#!/usr/bin/env bash
# mise-install.sh (__STACK__, linux) — install the runtimes declared in
# files/mise.toml (uploaded to ~/.config/mise/config.toml) as the unprivileged
# build user, then HARD-GATE the build on a smoke test so a missing tool fails
# the build instead of shipping a broken image. The darwin peer at
# ../darwin/mise-install.sh runs the same gate; add the __STACK__ payload to
# both if a runtime differs by platform, or keep them identical if it doesn't
# (see stacks/jvm/scripts/{linux,darwin}/mise-install.sh for a stack where
# nothing does). The shared mechanism (install/activate, smoke loop) is in
# /tmp/mise-lib.sh, uploaded by the Packer template.

set -euo pipefail
# /tmp/mise-lib.sh is staged on the guest by the Packer template (absent at lint time).
# shellcheck source=/dev/null
source /tmp/mise-lib.sh

mise_runtime_setup

# Smoke test (hard gate): one `--`-delimited group per tool row in the
# stack's `tools` declaration — argv tokens, not a quoted string.
# test/declaration.sh holds this call to set equality with those rows, so
# add the row and the group together. The corepack/pnpm/yarn groups are
# deliberate tripwires: mise_runtime_setup's corepack block skips silently
# when Corepack is absent, and Node 25+ no longer bundles it — this gate
# fails the build loudly when mise's `lts` alias floats past 24.
smoke_gate "__STACK__ runtimes" \
  -- node --version \
  -- corepack --version \
  -- command -v pnpm \
  -- command -v yarn

echo "==> mise-install.sh (__STACK__) complete."
