# Commands script/smoke runs over a NON-INTERACTIVE ssh to prove this stack's
# toolchain is reachable the way an agent or script reaches it (a login shell
# with no rc file). One per line; blank lines and # comments ignored.
#
# This is the runtime peer of mise-install.sh's build-time smoke_gate: that one
# proves the tool installed, this one proves it is still on PATH after imaging
# and cloning. Keep them aligned — a tool listed in neither is unverified.
node --version
