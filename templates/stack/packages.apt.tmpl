# __STACK__ native build dependencies (Debian/Ubuntu/apt names), one or more per line;
# '#' comments and blank lines ignored. Equivalent capabilities to packages.dnf
# (names differ); keep the two aligned. Empty if all runtimes are mise binaries.
