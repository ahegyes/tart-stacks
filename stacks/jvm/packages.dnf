# JVM stack native build dependencies (Fedora/dnf names); '#' comments and blanks
# ignored. Empty — all JVM runtimes are prebuilt mise binaries. Installs go through
# pkg_install_optional, which warns on a missing package instead of failing; the
# smoke gate in mise-install.sh is what catches a dropped capability — pair list
# changes with gate changes.
